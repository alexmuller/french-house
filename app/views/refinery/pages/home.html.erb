
<%= raw @page.content_for(:above_slideshow) %>

<ul id='gallery'>
  <% @page.images.each do |image| %>
    <li>
      <%= link_to image_tag(image.thumbnail("400x200#c").url), image.thumbnail("900x600").url %>
    </li>
 <% end %>
</ul>

<%= raw @page.content_for(:below_slideshow) %>
